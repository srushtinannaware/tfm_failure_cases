name: tfm-failure
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip

  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - scipy
  - seaborn

  - jupyterlab
  - notebook

  - pip:
      - tqdm
      - pyyaml